#include <bits/stdc++.h>
using namespace std;


int main() {
    int a , b;

    cin>>a>>b;

     // left shift

    cout << (a << 1) << endl; // 20
    cout << (a << 2) << endl; // 40

    // right shift

    cout << (a >> 1) << endl; // 5
    cout << (a >> 2) << endl; // 2

}